portal:
  url: "https://dati.comune.milano.it"  # URL base portale CKAN
  api_key: ""                            # Opzionale, per portali privati
  rows_per_page: 100                     # Dataset per richiesta paginata
  max_datasets: 0                          # 0 = nessun limite
  chunk_size: 0                            # 0 = file unico; >0 = N file TTL da chunk dataset ciascuno
  query_template: ""                     # Opzionale, filtro CKAN fq (es. "organization:nome-org")

catalog:
  uri: "https://dati.comune.milano.it/catalog"
  title: "Catalogo Open Data - Comune di Milano"
  description: "Catalogo dataset del Comune di Milano"
  issued: ""
  publisher_name: "Comune di Milano"
  publisher_identifier: "c_f205"         # Codice IPA — cerca su https://indicepa.gov.it
  language: "ITA"                        # Codice ISO 639-3
  homepage: "https://dati.comune.milano.it"

output:
  path: "output/milano.ttl"
